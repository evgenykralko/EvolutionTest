<Window x:Class="EvolutionTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EvolutionTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
		x:Name="MainForm">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="auto"/>
		</Grid.ColumnDefinitions>
		<Canvas Grid.Column="0" x:Name="canvas"/>
		<Border Grid.Column="1" Background="LightGray">
			<StackPanel>
				<StackPanel Orientation="Horizontal" Margin="2">
					<TextBlock Text="Epoch: " FontSize="16"/>
					<TextBlock Text="{Binding YearsCount}" FontSize="16"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="2">
					<TextBlock Text="Population: " FontSize="16"/>
					<TextBlock Text="{Binding EntitiesCount}" FontSize="16"/>
				</StackPanel>
			</StackPanel>

		</Border>
	</Grid>
</Window>
