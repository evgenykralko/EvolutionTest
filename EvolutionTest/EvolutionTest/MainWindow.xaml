<Window x:Class="EvolutionTest.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:EvolutionTest"
		mc:Ignorable="d"
		Title="MainWindow" Height="450" Width="800"
		x:Name="MainForm">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="auto"/>
		</Grid.ColumnDefinitions>
		<Canvas 
			Grid.Column="0" 
			x:Name="canvas"
			Background="AliceBlue"/>
		<Border Grid.Column="1" Background="LightGray">
			<StackPanel>
				<StackPanel Orientation="Horizontal" Margin="2">
					<TextBlock Text="Steps: " FontSize="16"/>
					<TextBlock x:Name="edSteps" FontSize="16"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="2">
					<TextBlock Text="Population: " FontSize="16"/>
					<TextBlock x:Name="edPopulation" FontSize="16"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="2">
					<TextBlock Text="Mode: " FontSize="16"/>
					<ComboBox SelectionChanged="ComboBox_SelectionChanged" 
						ItemsSource="{Binding Path=ColorModesList}"
						SelectedIndex="0"/>
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</Window>
